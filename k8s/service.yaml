apiVersion: v1
kind: Service
metadata:
  name: production-service
spec:
  type: LoadBalancer # 👈 Mudou aqui também
  selector:
    app: production-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3001